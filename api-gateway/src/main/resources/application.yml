spring:
  application:
    name: api-gateway
  cloud:
    gateway:
      routes:
        - id: auth-service
          uri: lb://auth-service
          predicates:
            - Path=/auth/**
        - id: user-service
          uri: lb://user-service
          predicates:
            - Path=/api/users/**,/internal/users/**,/api/teachers/**,/api/students/**
        - id: course-service
          uri: lb://course-service
          predicates:
            - Path=/api/courses/**,/api/enrollments/**,/api/lessons/**
        - id: upload-service
          uri: lb://upload-service
          predicates:
            - Path=/api/uploads/**
      globalcors:
        cors-configurations:
          '[/**]':
            allowedOriginPatterns: "*"
            allowedMethods:
              - GET
              - POST
              - PUT
              - DELETE
              - OPTIONS
            allowedHeaders: "*"
            maxAge: 3600

server:
  port: 8080

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
  instance:
    prefer-ip-address: true

logging:
  level:
    com.lms.gateway: DEBUG
    org.springframework.cloud.gateway: TRACE
